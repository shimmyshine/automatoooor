name: publish

on: [push]

jobs:
  publish-hello-world-image:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Login to GitHub Container Registry
      uses: docker/login-action@v1
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.CONTAINER_SECRET_DAMNIT }}

    - name: Build the automatoooor Docker image
      run: |
        docker build . --tag ghcr.io/shimmyshine/automatoooor:latest
        docker run ghcr.io/shimmyshine/automatoooor:latest
        docker push ghcr.io/shimmyshine/automatoooor:latest