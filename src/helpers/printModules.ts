import { Logger } from "tslog";
import { Modules } from "./Interfaces";

// eslint-disable-next-line @typescript-eslint/no-empty-function
const printModules = (log: Logger, functions: Modules): void => {};

export default printModules;
